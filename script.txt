cd fastapi; Remove-Item -Recurse -Force .\venv; python -m venv venv; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt; uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

cd HustBus_Backend; npm install; npx prisma generate; npx prisma migrate dev --name init; npm run dev

cd HustBus_FrontEnd; pnpm install; pnpm dev